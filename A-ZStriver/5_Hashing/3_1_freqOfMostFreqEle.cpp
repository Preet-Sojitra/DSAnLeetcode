#include <bits/stdc++.h>
using namespace std;

int maxFrequency(vector<int> &nums, int k)
{
    // Don't know how to do with hashmap. bit difficult :(
    return 0;
}
int main()
{
    vector<int> nums = {1, 2, 4};
    int k = 5;

    cout << maxFrequency(nums, k);
    return 0;
}